package main

import (
	"github.com/DoktorLenz/kp-wise23-24/quiz/go/user"
)

func main() {
	// sm := statemaschine.NewStateMachine()
	// sm.Start()
	_, _ = user.CreateUser("test", "test")
	// utils.Prompt("Created user: %+v", u)
	// check := u.CheckPassword("tesst")
	// utils.Prompt("Check password: %t", check)

	// u.Save()
}
